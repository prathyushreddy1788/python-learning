#Write a Python function that takes two strings as input and returns True if they are
# anagrams of each other (contain the same characters with the same frequency) and False otherwise.
# Ignore spaces, capitalization, and punctuation when checking for anagrams. For example,
# if the inputs are "Listen" and "Silent", the function should return True, but if the inputs
# are "Hello" and "World", the function should return False.

def check_anagram(input_String1,input_string2):
    input_String





